server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/clementeaguilar-usach/tingeso_pep2
          search-paths: config-data
  application:
    name: config-service